pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.quarkus' version '3.16.4'
        id "io.freefair.lombok" version "$lombokPluginVersion"
    }
}
rootProject.name='transactions-api' 